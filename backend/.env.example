# Template only — copy to backend/.env and fill in real values there.
# NEVER put a real key in this file: .env.example IS committed to git.
# (A real key was previously committed here and had to be auto-revoked.)

ANTHROPIC_API_KEY=sk-ant-your-key-here
PORT=5050
ADMIN_PASSCODE=0000
NODE_ENV=development
